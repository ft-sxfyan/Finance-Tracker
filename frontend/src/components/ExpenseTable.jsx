import { formatCurrency, formatDate } from '../utils/formatters';
function ExpenseTable({ expenses = [], onEditExpense, onDeleteExpense, emptyMessage = 'No expenses found.' }) { return expenses.length ? <div className="table-wrap"><table><thead><tr><th>Date</th><th>Item</th><th>Category</th><th>Payment</th><th>Amount</th><th>Actions</th></tr></thead><tbody>{expenses.map((expense) => <tr key={expense._id || expense.id}><td>{formatDate(expense.date)}</td><td>{expense.item}</td><td>{expense.category}</td><td>{expense.paymentMethod}</td><td>{formatCurrency(expense.amount)}</td><td className="button-row">{onEditExpense && <button className="button-quiet" onClick={() => onEditExpense(expense)}>Edit</button>}<button className="button-danger" onClick={() => onDeleteExpense(expense._id || expense.id)}>Delete</button></td></tr>)}</tbody></table></div> : <p className="empty-state">{emptyMessage}</p>; }
export default ExpenseTable;
